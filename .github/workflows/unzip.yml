name: Descompactar ZIP

on:
  workflow_dispatch:

jobs:
  unzip_job:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Acessar o repositório
        uses: actions/checkout@v3

      - name: Instalar unzip
        run: sudo apt-get update && sudo apt-get install -y unzip

      - name: Listar arquivos antes do unzip
        run: ls -la

      - name: Descompactar o arquivo ZIP
        run: unzip -o 20matar.zip -d arquivos

      - name: Verificar conteúdo descompactado
        run: ls -la arquivos

      - name: Commit e push dos arquivos
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add arquivos/
          git diff --cached --quiet || git commit -m "Arquivos extraídos automaticamente"
          git push
